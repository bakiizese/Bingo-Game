const EvaluateGame = (GameRules, clicked, storedData) => {
  // console.log(GameRules);
  // console.log(storedData);
  // console.log(clicked);
  ///send data to back end then gets response on who is winning and a sort
};

export default EvaluateGame;
